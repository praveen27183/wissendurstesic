import React, { useState } from "react";

const Quizzes = () => {
  const [quizzes, setQuizzes] = useState([]);

  return (
    <div>
      <h1>Quizzes</h1>
    </div>
  );
};

export default Quizzes;